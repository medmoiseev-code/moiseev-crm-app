{
  "name": "stomx-crm-demo",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
